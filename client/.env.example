# Keep this empty to use the Vite API proxy.
# VITE_API_BASE_URL=

# Change only when the backend uses a non-default local port, for example http://localhost:5051.
VITE_API_PROXY_TARGET=http://localhost:5000
